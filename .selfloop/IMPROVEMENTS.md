# Self-loop improvement log (append-only)

One line per loop iteration:
`[ISO-timestamp] loop=N | defect: <one sentence> | fix: <what changed, which file> | verified: <how>`
[2026-07-14T03:14:55Z] loop=1 | defect: templates/CHANGELOG.md format line said 'Touched: <feature IDs> → <resulting dots>', stale vocabulary from the retired colored-dot status system that contradicts the word statuses (PLANNED/UNVERIFIED/…) used in its own sample entry and everywhere else | fix: changed '<resulting dots>' to '<resulting statuses>' in templates/CHANGELOG.md | verified: grep for 'dot' across product files returns nothing; emoji-vocab check still clean; sample entries still use word statuses
[2026-07-14T03:20:00Z] loop=2 | defect: install.sh gated BOTH gitignore lines on whether '.env' was already present, so a target repo that already ignored .env never got 'greenlight/state/' added and would commit runtime state | fix: split into two independent grep-guarded appends (one per pattern) in install.sh | verified: bash -n passes; tempdir test of 3 scenarios (no .gitignore, existing .env-only .gitignore, re-run) all end with both lines exactly once
[2026-07-13T00:00:00Z] loop=3 | defect: templates/CONTROL.md rung 7 (ROLLOVER) fired whenever all features were STABLE, which is also rung 9 (DONE)'s precondition — being higher priority it would bump the major version and demote everything forever, so DONE was unreachable and the loop never terminated | fix: gated ROLLOVER on version still 0.x, making it the one-time v1.0 full-retest milestone (matches README and the streak example) | verified: re-read the ladder — at >=1.0 all-STABLE now falls through to IDEATE/DONE; grep shows no other file describes repeated major rollovers; emoji + placeholder checks still clean
[2026-07-14T03:56:13Z] loop=4 | defect: rung 8 IDEATE fired on every all-STABLE state (its "nothing pending" precondition is permanently true there) and sits above rung 9 DONE, so it filed 3 fresh ideas forever while DONE's "no pending ideas" clause was falsified by those very ideas (default autonomous_feature_add: false leaves them "pending review" indefinitely) — GREENLIGHT ACHIEVED was unreachable, contradicting the README promise that the loop stops itself | fix: made IDEATE one-shot ("no idea round filed since the last feature change", ideas parked for the human explicitly do NOT block DONE) and changed DONE's precondition from "no pending ideas" to "the IDEATE round already filed", in templates/CONTROL.md | verified: traced the ladder at v1.x/all-STABLE — IDEATE fires once, next loop falls through to DONE; grep confirms the stale "no pending ideas" clause is gone and the ladder text exists only in templates/CONTROL.md (examples/streak defers to it); shell/placeholder/emoji/.greenlight checks all still clean
[2026-07-13T00:00:00Z] loop=5 | defect: bootstrap.md told the agent to "fill"/"seed"/"generate" CONTROL/LEDGER/DECISIONS/CHANGELOG but never said to start them from greenlight/templates/ (its only copy instruction covered STANDARDS.md + STACKS.md), so a fresh agent would invent CONTROL.md from scratch and could omit the §0 rung ladder and front-matter keys — making loop.md's core instruction "Walk CONTROL.md §0 rungs top-down" impossible to follow and wasting the four templates install.sh downloads | fix: rewrote the "Both paths" closing line in prompts/bootstrap.md to require copying each doc from greenlight/templates/ first, keeping CONTROL.md's §0 rules and front-matter verbatim and replacing only placeholders/sample rows, while noting DESIGN.md and the app README have no template | verified: all six files bootstrap now names exist under templates/ and are downloaded by install.sh (scripted check); no contradiction with §B3 which writes DESIGN.md/README fresh; bash -n, examples-placeholder, emoji and .greenlight checks all still clean
[2026-07-13T00:00:00Z] loop=6 | defect: rungs 7 (ROLLOVER) and 9 (DONE) both fire on "all STABLE", but RETIRED is a permanent status a feature can never leave — so any project that ever retires a feature makes "all STABLE" false forever and the loop deadlocks (nothing UNVERIFIED/PLANNED/hardenable, ROLLOVER skipped, DONE skipped, spins doing nothing); examples/streak proves the text contradicts intent, retiring F-006 at loop 2 yet still rolling over to v1.0.0 at loop 12 | fix: defined the term once in the Statuses paragraph of templates/CONTROL.md — "all STABLE" = every non-RETIRED feature is STABLE, RETIRED excluded from every count, BLOCKED is not STABLE and holds DONE until the human answers | verified: grep confirms templates/CONTROL.md is the only file defining the phrase (examples merely use it); re-traced the ladder against the streak example — ROLLOVER now fires at loop 12 demoting exactly the 4 live features as its LEDGER records, and DONE is reachable once live features stabilize; shell, examples-placeholder, emoji and .greenlight checks all still clean
[2026-07-13T00:00:00Z] loop=7 | defect: examples/streak (the flagship worked example) told the loop it would reach DONE/greenlight as soon as F-003/UX-001/F-004 hit STABLE, while F-005 sat BLOCKED — directly contradicting templates/CONTROL.md §0 ("a BLOCKED feature is *not* STABLE and holds DONE until the human answers"), so an agent pattern-matching the example would flip greenlight: "yes" with an unanswered human ask outstanding | fix: §5 Next Action in examples/streak/CONTROL.md and the loop-24 "Next:" line in examples/streak/CHANGELOG.md now state that BLOCKED is not STABLE and F-005 holds the DONE rung until the human answers in DECISIONS and it is built to STABLE or RETIRED | verified: grep shows the two DONE/greenlight predictions in examples/ are the only ones and both now name the blocker; shell parse, examples-placeholder, emoji-vocab and .greenlight checks all still clean
[2026-07-13T00:00:00Z] loop=8 | defect: bootstrap.md told the human to paste the idea dump "at the bottom" of the prompt file while README Part 2 runs `claude "$(cat greenlight/prompts/bootstrap.md)"` and promises the agent will ask for the braindump in chat — so on the documented path the agent receives the literal `{{paste here}}` placeholder, yet §B1 opens with "Reflect my dump back" and no rule covers an empty dump, leaving a fresh agent to stall or invent the app idea in the one phase the file says "determines everything downstream" | fix: prompts/bootstrap.md header now offers both entry paths (paste in the file OR paste in chat when asked), and §B1 opens with "if the dump is empty or still the {{paste here}} placeholder, ask me for it and wait — never invent the idea yourself" | verified: README Part 2 and bootstrap.md now describe the same flow; bootstrap keeps its legitimate {{...}} placeholders; shell parse, examples-placeholder, emoji-vocab and .greenlight checks all still clean
[2026-07-13T00:00:00Z] loop=9 | defect: no rung in templates/CONTROL.md §0 ever un-blocked a BLOCKED feature — rung 3 covered only "failing" features and rung 5 only PLANNED ones, so once the human answered the DECISIONS ask nothing picked the answer up: the feature stayed BLOCKED forever and (per the "BLOCKED is not STABLE" rule) held DONE forever, contradicting README ("the next iteration unblocks") and examples/streak §5 (F-005 "is built to STABLE or RETIRED"), which the ladder had no path to do | fix: extended rung 3 to "BROKEN / UNBLOCK" in templates/CONTROL.md — a BLOCKED feature whose ask the human has answered gets the answer applied that loop (Contract written + set PLANNED for rung 5, or RETIRED), and only an unanswered ask keeps it BLOCKED | verified: rung numbering still 1-9 so all rung/§ cross-references hold; traced streak F-005 end-to-end (answer → rung 3 unblock → rung 5 build → VERIFY → HARDEN → STABLE → DONE reachable); shell parse, examples-placeholder, emoji-vocab and .greenlight checks all still clean
[2026-07-14T04:09:03Z] loop=10 | defect: rung 1 SECURITY fired on ANY unchecked §4 item and outranks rung 5 BUILD, but a fresh project starts with all 8 Security Baseline boxes unchecked and several are impossible to satisfy before the code exists (rate limiting with no endpoints, "auth enforced on EVERY route" with no routes) — so read literally the loop could never reach BUILD and would either spin on an unbuildable item or tick boxes it had not earned; §4 ("all boxes required before COMPLETE") and rung 9 already make the full checklist the COMPLETION gate, and examples/streak contradicts the text by building F-001..F-003 at loops 3-11 and only adding rate limiting at loop 15 | fix: scoped rung 1 in templates/CONTROL.md to a security regression OR an unchecked §4 item that is actionable now (the code it governs exists), with unbuildable items left unchecked to fall through, and restated that §4 must be fully checked before DONE rather than before every BUILD | verified: rung numbering still 1-9 so the "rung 5" and "rungs 7 and 9" cross-references still hold; traced a fresh project (only bootstrap-doable items actionable -> falls through to BUILD) and the streak timeline (rung 1 correctly preempts at loop 15 once the endpoint exists, DONE still gated on the full checklist); shell parse, examples-placeholder, emoji-vocab and .greenlight checks all still clean
[2026-07-14T04:11:02Z] loop=11 | defect: rung 8 IDEATE re-armed on "no idea round filed since the last feature change" — with autonomous_feature_add: true the round files ideas and builds one, which IS a feature change, so once that feature stabilized IDEATE (above DONE) fired again, filed 3 more ideas, built another, forever: GREENLIGHT ACHIEVED was unreachable for every project that enables the flag, contradicting the README promise that the loop stops itself | fix: made IDEATE fire once ever per project ("no idea round has been filed yet in this project ... never re-arm, not even after the ideas themselves change the app") in templates/CONTROL.md | verified: traced both configs at all-STABLE — false: files once then DONE (unchanged); true: files once, builds the idea, feature stabilizes, IDEATE no longer re-arms, falls through to DONE; rung numbering still 1-9 so the "rung 5" and "rungs 7 and 9" cross-references hold; grep confirms no other file restates the old clause; shell parse, examples-placeholder, emoji-vocab and .greenlight checks all still clean
[2026-07-14T04:14:00Z] loop=12 | defect: templates/CONTROL.md defined "all STABLE" once for BOTH rung 7 (ROLLOVER) and rung 9 (DONE) with "a BLOCKED feature is *not* STABLE", so any project holding one unanswered human ask could never satisfy rung 7 and was pinned below v1.0 forever — never reaching the v1.0 full-retest milestone the README promises; examples/streak proves the rule contradicts its own intent, keeping F-005 BLOCKED from loop 5 through loop 24 yet rolling over to v1.0.0 at loop 12 | fix: split the definition per rung in templates/CONTROL.md — BLOCKED is excluded from rung 7 (no contract yet, so nothing for a retest milestone to re-prove) but still excluded from nothing at rung 9, where it holds DONE until the human answers and rung 3 takes it to STABLE or RETIRED | verified: grep confirms templates/CONTROL.md is the only file defining the phrase (examples only use it) and the DONE-side rule other loops depend on is unchanged; rung numbering still 1-9 so the rung 3/5/7/9 cross-references hold; traced streak (loop 12 rollover now legal with F-005 BLOCKED; §5 Next Action still correctly says F-005 holds DONE); shell parse, examples-placeholder, emoji-vocab and .greenlight checks all still clean
[2026-07-14T04:16:11Z] loop=13 | defect: templates/CONTROL.md described the `notify` front-matter key as "how the harness pings you at COMPLETE/BLOCKED", but run-loop.example.sh never reads CONTROL.md — its notify() hardcodes a macOS desktop notification and its own comment says other channels are wired by hand-editing the function body, so a human who set notify: "ntfy-url" would get no ntfy pings and no clue why | fix: rewrote the key comment in templates/CONTROL.md to say it records the channel you want and that notify() in greenlight/run-loop.sh must be edited to match (it ships with desktop notifications) | verified: grep shows templates/CONTROL.md is the only file that describes the key; key name and value list unchanged so examples/streak/CONTROL.md (notify: "file") still matches the template front-matter; shell parse, examples-placeholder, emoji-vocab and .greenlight checks all still clean
[2026-07-14T04:18:32Z] loop=14 | defect: install.sh appended its two .gitignore lines with `printf ... >> .gitignore` without checking that the existing file ended in a newline, so a target repo whose .gitignore has no trailing newline (common — many editors and generators omit it) got the lines fused onto the last one ("node_modules.env"), silently un-ignoring BOTH that pattern and `.env` itself — the exact failure the line exists to prevent, letting the loop commit real secrets | fix: added a guard before the appends in install.sh — if .gitignore is non-empty and its last byte is not a newline, append one first (written as an `if` so the false branch cannot trip `set -e`) | verified: bash -n passes; exercised the block under `set -euo pipefail` in 4 tempdir scenarios (no .gitignore / no-trailing-newline / .env-only / already-installed re-run) — every one ends with `.env` and `greenlight/state/` as their own lines exactly once, rc=0, no blank-line churn and no clobbered pattern; placeholder, emoji-vocab and .greenlight checks still clean
[2026-07-14T04:20:27Z] loop=15 | defect: front-matter `status` documented a lifecycle (BOOTSTRAP -> BUILDING -> HARDENING -> COMPLETE) that NO instruction ever advanced — grep shows BUILDING/HARDENING appear in no rung, no closing step and no prompt, and the only writer is rung 9 (COMPLETE), so every real project would sit at "BOOTSTRAP" from bootstrap until greenlight while the flagship examples/streak shows status: "HARDENING" at loop 24, a value the documented rules cannot produce | fix: templates/CONTROL.md — defined each value from the §3 board at the key (BOOTSTRAP = every feature still PLANNED; BUILDING = some built, some still PLANNED; HARDENING = none left PLANNED, still verifying/hardening; COMPLETE = set by the DONE rung) and added `status` re-evaluated from the board to the "Before finishing every loop" front-matter update | verified: streak now derives HARDENING from its own board (no PLANNED rows: F-005 BLOCKED, F-006 RETIRED, rest STABLE/PASSING); rung numbering still 1-9 so the rung 3/5/7/9 cross-references hold; grep confirms templates/CONTROL.md is the only file defining the key and loop.md step 5 (status: COMPLETE) still agrees with rung 9; shell parse, examples-placeholder, emoji-vocab and .greenlight checks all still clean
[2026-07-13T00:00:00Z] loop=16 | defect: nothing in the product ever bumped `version` except the one-time ROLLOVER rung — no rung, closing step or prompt defined a minor/patch bump — yet rung 2 REGRESSION ("find the break version in LEDGER, read that CHANGELOG entry, fix from that lead"), templates/LEDGER.md's header ("BROKEN at v1.4 -> check CHANGELOG v1.4") and README ("the loop sees exactly which version broke it") all depend on it advancing: under the documented rules every LEDGER line and CHANGELOG heading reads v0.1.0 forever, so the break-version lookup resolves to every entry at once and the ladder's #2 rung is inert; examples/streak shows v0.2.0/v1.1.1/v1.2.2, values no rule can produce | fix: templates/CONTROL.md — defined the bump at the `version` key (MINOR when a BUILD merges a feature, PATCH when a fix lands at rungs 1-3, MAJOR only at the v1.0.0 ROLLOVER, no bump for VERIFY/HARDEN) and put "bump version first, since CHANGELOG and LEDGER both record it" at the head of the "Before finishing every loop" sequence; examples/streak loop 22 built F-004 (a feature) but bumped only a patch, contradicting the new rule, so its loops 22-24 are now v1.3.0 in CONTROL front-matter, CHANGELOG headings and LEDGER lines | verified: walked all 24 streak loops against the rule — builds (3,5,7,15,18,22) minor, fixes (17,20) patch, rollover (12) major, verify/harden none — LEDGER v= now agrees with the CHANGELOG heading at every loop and front-matter equals the latest; traced rung 2 end-to-end (F-001 BROKEN at v=1.1.0 -> CHANGELOG v1.1.0 = "added rate limiting" = the lead); rung numbering still 1-9 so the rung 3/5/7/9 cross-references hold; shell parse, examples-placeholder, emoji-vocab and .greenlight checks all still clean
[2026-07-14T04:26:25Z] loop=17 | defect: the version-bump rule in templates/CONTROL.md keyed the bump on WHICH RUNG fired ("PATCH when a fix lands (rungs 1-3)"), but rung 1 SECURITY does not only land fixes — since loop 10 it explicitly builds an unchecked §4 baseline item whose code does not exist yet, i.e. a brand-new registry feature; examples/streak loop 15 does exactly that (adds rate limiting SEC-002 under "Why: Security rung") and bumps v1.0.0 -> v1.1.0, a MINOR the rule literally forbids, so template and flagship example taught opposite versioning and a fresh agent would have written v1.0.1 | fix: templates/CONTROL.md version key now bumps by what the change DID, not the rung — MINOR when a new registry feature merges (including one built at the SECURITY rung, e.g. rate limiting), PATCH when the change only fixes existing behavior | verified: replayed all 24 streak loops — new features (3,5,7,15,18,22) minor, pure fixes (17,20) patch, rollover (12) major, verify/harden/retire (rest) no bump — CHANGELOG headings, LEDGER v= and CONTROL front-matter now agree at every loop with no example edits needed; grep confirms templates/CONTROL.md is still the only file defining the rule; rung numbering still 1-9 so the rung 3/5/7/9 cross-references hold; shell parse, examples-placeholder, emoji-vocab and .greenlight checks all still clean
[2026-07-14T04:28:25Z] loop=18 | defect: loop.md declares "bare filenames below mean those `greenlight/` copies" and its greenlight-docs list contains no README, yet its mandatory Close step said "update README.md now" — so read under its own naming rule the loop would write a `greenlight/README.md` that no other product file references, leaving the app README (STANDARDS: the human-facing derivative of the registry; bootstrap: "README.md for the app") permanently stale; bootstrap disambiguates the app README twice, loop.md never did | fix: prompts/loop.md step 4 now says "update the app's README.md — the one at the repo ROOT, not a `greenlight/` file" | verified: grep shows loop.md step 4 is the only README reference in prompts/ and it now matches bootstrap §B3.4 and its no-template note; single-folder rule intact (root README is the app, not a Greenlight doc); shell parse, examples-placeholder, emoji-vocab and .greenlight checks all still clean
[2026-07-14T04:30:58Z] loop=19 | defect: templates/CONTROL.md §4 told the agent to "skip N/A items per Profile ... and note the skip" (i.e. leave the box UNCHECKED), but rung 9 DONE requires "§4 fully checked" — so any project whose Profile makes even one baseline item N/A (persistence: none → no parameterized-query rules; a CLI or local-only app → no public endpoints to rate-limit) could never satisfy the gate: the N/A item is also not actionable so rung 1 correctly falls through, leaving nothing that could ever check the box and making GREENLIGHT ACHIEVED unreachable forever | fix: rewrote the §4 parenthetical in templates/CONTROL.md — an N/A item COUNTS AS SATISFIED (check the box, append "N/A: <why>"), while a box that DOES apply and is not done stays unchecked for the SECURITY rung | verified: traced both profiles — CLI/no-DB now checks its N/A boxes with a reason and reaches DONE, while a web+postgres app still leaves real unmet boxes unchecked for rung 1 (behavior unchanged); consistent with bootstrap A2 ("leave UNMET boxes unchecked" — unmet is not N/A) and with §4's "all boxes required before COMPLETE" header; examples/streak needs no edit (all 8 items apply to its web/auth/postgres profile and are already checked); grep confirms templates/CONTROL.md is still the only file defining the convention; rung numbering still 1-9 so the rung 3/5/7/9 cross-references hold; shell parse, examples-placeholder, emoji-vocab and .greenlight checks all still clean
[2026-07-14T04:33:34Z] loop=20 | defect: no product file ever told the loop to `git commit` — grep shows the only git instruction in prompts/ or templates/ is loop.md step 1 READING `git log`, yet the system depends on one commit per iteration: README promises "Every iteration ends in a commit — this is the loop's diary", tells the human to `git revert <commit-id>` an iteration and to push mid-flight, loop.md step 1 uses `git log` as cross-loop memory, and CHANGELOG/LEDGER both carry a Commits field; only a BUILD loop implicitly commits (step 2: branch -> merge), so VERIFY/HARDEN/ROLLOVER/IDEATE/UNBLOCK/DONE loops change docs only and their work was never committed — git log stayed empty, the CHANGELOG Commits field had nothing to name, and uncommitted doc edits accumulated in the working tree across iterations | fix: prompts/loop.md step 4 (Close, mandatory) now ends with "Finally `git commit` everything this loop touched (code, docs, README) with a message naming the loop number and rung — every iteration ends in a commit and leaves nothing uncommitted" | verified: grep confirms loop.md is now the only file instructing the commit and every README claim (diary, revert-an-iteration, push, safe window) and both ledger Commits fields now have an instruction that produces them; Close is mandatory on every rung so doc-only loops commit too; build loops still branch/merge per step 2; shell parse, examples-placeholder, emoji-vocab and .greenlight checks all still clean
[2026-07-14T04:36:01Z] loop=21 | defect: rung 9 (DONE) in templates/CONTROL.md gated GREENLIGHT on "fresh UI evidence" as a flat conjunct, but the Profile explicitly supports non-UI surfaces (`surface: cli` / `api`, both of which STACKS.md and STANDARDS.md have first-class sections for) — such a project can never produce UI evidence, so the final rung was unsatisfiable and the loop would spin below DONE forever (same unreachable-gate class as the N/A security boxes fixed in loop 19); every OTHER statement of the rule is correctly scoped ("UI features need an interaction pass" in loop.md step 3, "UI features additionally require a fresh screenshot" in CONTROL §0 Statuses), so rung 9 was also the lone place a fresh agent had to guess | fix: rung 9 now reads "fresh UI evidence for every UI feature (a Profile with no UI — e.g. `surface: cli` or `api` — has no UI features, so this clause is already satisfied)" in templates/CONTROL.md | verified: traced DONE for a CLI profile (no UI features -> clause satisfied, gate now turns only on all-STABLE + §4 + IDEATE) and for the web streak example (unchanged — its UI features carry fresh screenshot evidence, §5 Next Action still correct); rung numbering still 1-9 so the rung 3/5/7/9 cross-references hold; grep confirms templates/CONTROL.md rung 9 is the only place the gate is stated; shell parse, examples-placeholder, emoji-vocab and .greenlight checks all still clean
